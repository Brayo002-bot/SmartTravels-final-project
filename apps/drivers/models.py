from django.db import models


# The driver records used by the system are defined in apps.buses.models.Driver.
# This app provides driver-facing views and routing, while the bus driver
# entity itself is maintained in the buses app.
